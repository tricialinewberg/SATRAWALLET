# Copy this file to .env and fill in real values. Never commit .env.

# Breez SDK - Nodeless (Spark) API key.
# Used to initialize the Lightning wallet backend (breez_sdk_spark_flutter).
BREEZ_API_KEY=
