name: calculadora
description: A new Flutter project.
publish_to: none
# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.9

  # Bitcoin Lightning wallet SDK (Breez, Nodeless/Spark implementation).
  # Not published on pub.dev yet — pulled directly from the official repo,
  # pinned to a release tag so builds are reproducible.
  breez_sdk_spark_flutter:
    git:
      url: https://github.com/breez/breez-sdk-spark-flutter
      ref: v0.19.0

  # Nostr protocol client (NIP-17/44/59 support) for the trusted-contacts escape alert.
  nostr: ^2.0.0

  # NFC read/write for the physical recovery key. nfc_manager 4.x split the
  # cross-platform NDEF abstraction out into its own companion package.
  nfc_manager: ^4.2.1
  nfc_manager_ndef: ^1.1.0

  # Encrypted local storage (Android Keystore / iOS Keychain) for the PIN,
  # the wallet's recovery mnemonic, and Nostr keys.
  flutter_secure_storage: ^10.3.1

  # Optional fingerprint/Face ID unlock. The PIN remains available as fallback.
  local_auth: ^3.0.2

  # BIP-39 mnemonic generation/validation for the wallet's recovery phrase.
  # Maintained fork of the (now unmaintained) `bip39` package.
  bip39_plus: ^1.1.1

  # HMAC-SHA512 (crypto) and secp256k1 point math (elliptic) for the
  # from-scratch NIP-06 (BIP-32) Nostr key derivation in lib/services/nip06.dart.
  # Both were already present transitively (via bip39_plus and nostr,
  # respectively) — declared directly here since nip06.dart depends on
  # their APIs and shouldn't rely on another package's transitive pin.
  crypto: ^3.0.6
  elliptic: ^0.3.12

  # QR code rendering for the receive screen.
  qr_flutter: ^4.1.0

  # Camera-based QR reader for the send screen.
  mobile_scanner: ^7.1.3

  # App-sandboxed storage directory for the Breez SDK's local data.
  path_provider: ^2.1.6
  cryptography: ^2.9.0
  url_launcher: ^6.3.2

  # Keeps the inheritance test observer alive on Android while the app is
  # minimized. Android displays a persistent notification while it runs.
  flutter_foreground_task: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.11.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #  - images/a_dot_burr.jpeg
  #  - images/a_dot_ham.jpeg
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
